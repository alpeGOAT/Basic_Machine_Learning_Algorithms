# Gaussian Mixture Model with the Wine Dataset
This project applies GMM to the scikit-learn Wine dataset. Because the original dataset has 13 features, StandardScaler is used to normalize the values and PCA reduces the data to two combined features for visualization. 
A three-component GMM then groups the wine samples and displays the calculated cluster means on a two-dimensional graph.
