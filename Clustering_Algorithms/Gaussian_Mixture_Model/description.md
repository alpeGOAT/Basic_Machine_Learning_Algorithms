# Gaussian Mixture Model with a Sample Dataset
This project applies a Gaussian Mixture Model to an artificial dataset with 500 points and three groups. GMM is a soft clustering method, so it calculates the probability that each point belongs to every cluster. 
A full covariance type is used, allowing clusters to have flexible shapes. The model is trained, the most likely cluster is assigned to each point, and the cluster means are displayed as centers.
