# Fuzzy-C Using Wine Dataset
This project applies Fuzzy C-Means to the Wine dataset. The 13 original features are standardized and reduced to two features using PCA. 
The algorithm creates three fuzzy clusters and calculates the membership strength of every wine sample. The point's strongest membership is also used to display a hard cluster, while the cluster centers are shown separately.
