# Grocery Shopping Dataset Project
This project uses a grocery transaction dataset to discover which products are commonly purchased together. 
Purchases are grouped by customer and date so that every shopping visit becomes a transaction. TransactionEncoder converts the product lists into True/False values, and the Apriori algorithm finds frequent itemsets. 
Association rules are then generated using support, confidence, and lift. The project also identifies and visualizes the ten most frequently purchased grocery items.
